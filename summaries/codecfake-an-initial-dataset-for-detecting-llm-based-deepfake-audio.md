# Codecfake: An Initial Dataset for Detecting LLM-based Deepfake Audio

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2406.08112 · Yi Lu et al. · 2024

## 한 줄 요약

본 논문에서는 LLM 기반 오디오 딥페이크 탐지에 활용할 수 있는 Codecfake 데이터셋을 제안하고, 이를 통해 학습된 ADD 모델이 기존 vocoder 기반 모델보다 우수한 성능을 보임을 실험적으로 입증합니다.

## 문제 정의

LLM 기반 오디오 딥페이크의 등장으로 인해 효과적인 탐지 방법의 필요성이 대두되고 있습니다. 기존의 딥페이크 오디오 탐지 모델은 주로 vocoder를 활용한 오디오 생성 과정을 분석하지만, LLM 기반 오디오는 vocoder를 거치지 않고 직접 생성되므로 이러한 모델의 한계가 발생합니다. 따라서 LLM 기반 오디오 딥페이크 탐지에 특화된 새로운 접근 방식과 데이터셋의 필요성이 제기됩니다.

## 제안 방법

본 논문에서는 LLM 기반 오디오 딥페이크 탐지를 위해 Codecfake 데이터셋을 구축합니다. 이 데이터셋은 seven representative neural codec methods를 사용하여 생성된 인위적인 오디오 샘플로 구성됩니다. 제안하는 방법은 이 데이터셋을 사용하여 ADD 모델을 학습하고, 기존의 vocoder 기반 ADD 모델과 비교하여 성능을 평가하는 것입니다.

## 실험·결과

실험에서는 세 가지 대표적인 ADD 모델(Mel-LCNN, W2V2-LCNN, W2V2-AASIST)을 사용하여 Codecfake 데이터셋으로 학습하고, 기존의 vocoder 기반 ADD 모델(ASVspoof2019LA 데이터셋으로 학습)과 비교했습니다. 그 결과, Codecfake 데이터셋으로 학습된 ADD 모델은 vocoder 기반 모델에 비해 평균 EER에서 41.406% 감소하는 성능 향상을 보였습니다. 특히, W2V2-AASIST 모델은 0.177%의 낮은 EER을 달성하여 우수한 성능을 입증했습니다.

## 한계

본 연구는 주로 7가지 대표적인 신경 코덱 모델에 초점을 맞추었으므로, 이 외의 다른 코덱 모델에 대한 일반화 성능은 아직 검증되지 않았습니다. 또한, Codecfake 데이터셋은 실제 환경에서 발생할 수 있는 다양한 노이즈나 변형을 포함하지 않을 수 있으며, 이는 실제 딥페이크 오디오 탐지 성능에 영향을 미칠 수 있습니다. 더 나아가, 본 연구는 주로 ID 환경에서의 성능을 평가했으며, unseen codec 또는 다른 형태의 딥페이크 오디오에 대한 성능은 추가적인 연구가 필요합니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
