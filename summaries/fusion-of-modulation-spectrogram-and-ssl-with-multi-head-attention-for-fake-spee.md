# Fusion of Modulation Spectrogram and ssl with Multi-Head Attention for Fake Speech Detection

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2508.01034 · Rishith Sadashiv et al. · 2025

## 한 줄 요약

본 논문에서는 딥페이크 음성 검출(Fake Speech Detection, FSD)의 도메인 일반화 문제를 해결하기 위해, 음성 특징으로 modulation spectrogram과 self-supervised learning (SSL) 임베딩을 결합하는 새로운 front-end 표현을 제안합니다.

## 문제 정의

기존의 딥페이크 음성 검출 시스템은 학습 데이터와 다른 도메인의 음성 샘플에 대해 일반화 능력이 부족합니다. 이는 학습 데이터의 다양성 부족으로 인해 발생하며, 다양한 녹음 조건 및 데이터셋 특성 변화에 민감하게 반응하여 성능 저하를 초래합니다. 따라서 도메인 일반화 능력을 향상시키는 것이 중요한 과제로 남아있습니다.

## 제안 방법

본 연구에서는 음성 특징으로 modulation spectrogram과 self-supervised learning (SSL) 임베딩을 결합하여 도메인 일반화 능력을 향상시키는 새로운 front-end 표현을 제안합니다. wav2vec 2.0 XLS-R 모델을 활용한 cross-lingual SSL 임베딩과 modulation spectrogram 특징을 multi-head attention 메커니즘을 통해 결합합니다. 이 결합된 특징은 AASIST back-end 네트워크의 입력으로 사용되어 딥페이크 음성 검출을 수행합니다.

## 실험·결과

제안된 모델은 AVSspoof 2019 및 MLAAD 데이터셋에서 baseline 모델 대비 상당한 성능 향상을 보였습니다. 특히, AVSspoof 2019 데이터셋에서 학습된 모델은 MLAAD 데이터셋에서 36%의 상대적인 성능 향상을 나타냈습니다. 또한, 다양한 언어 데이터셋에 대한 실험 결과, 제안된 모델은 일관되게 baseline 모델보다 우수한 성능을 보이며, 이는 더 나은 도메인 일반화 능력을 입증합니다.

## 한계

본 연구에서는 modulation spectrogram과 SSL 임베딩의 결합에 초점을 맞추었으며, 다른 다양한 음성 특징이나 고급 학습 전략의 통합은 고려하지 않았습니다. 또한, 제안된 모델의 계산 복잡성에 대한 분석은 부족하며, 실시간 시스템 적용 시의 효율성에 대한 추가적인 연구가 필요합니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
