# Partially Fake Audio Detection by Self-Attention-Based Fake Span Discovery

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2202.06684 · Haibin Wu et al. · 2022

## 한 줄 요약

본 논문에서는 부분적으로 조작된 오디오를 탐지하기 위해 질문-답변 전략과 self-attention 메커니즘을 결합한 새로운 프레임워크를 제안하며, ADD 2022 부분적으로 조작된 오디오 탐지 트랙에서 2위를 기록했다.

## 문제 정의

음성 합성 및 변환 기술의 발전으로 인해 생체 인식 모델의 견고성이 약화되고, 악의적인 공격자가 이를 악용하여 불법적인 목적으로 사용할 수 있는 우려가 증가하고 있다. 특히, 실제 사용자 음성 속에 작은 가짜 클립을 삽입하는 부분적으로 조작된 오디오 공격은 기존의 스푸핑 방어 모델로 탐지하기 어렵다는 문제가 있다.

## 제안 방법

본 논문에서는 부분적으로 조작된 오디오 탐지를 위해 질문-답변 전략을 활용하는 새로운 프레임워크를 제안한다. 이 프레임워크는 입력 오디오 내에서 가짜 클립의 시작 및 종료 위치를 예측하도록 모델을 학습시킨다. 또한, self-attention 메커니즘을 도입하여 모델이 가짜 클립 탐지에 집중하도록 유도하고, 실제 오디오와 부분적으로 조작된 오디오 간의 판별 능력을 향상시킨다.

## 실험·결과

실험에서는 다양한 특징(MFFT, MFC, LFCC, SinCNet), FFT window size (384-768), pooling 전략 (ASP, SAP), 데이터 증강 및 재합성 데이터 사용 여부 등 다양한 실험 설정을 통해 제안 방법의 성능을 평가했다. 그 결과, self-attention 메커니즘을 추가했을 때 EER이 크게 감소했으며, 특히 LFCC 특징을 사용한 모델이 가장 높은 성능을 보였다. 또한, 5개 모델의 평균 점수로 융합한 모델이 가장 좋은 성능을 달성했다.

## 한계

본 연구는 부분적으로 조작된 오디오 탐지에 초점을 맞추고 있으며, 다른 유형의 스푸핑 공격이나 다양한 환경에서의 성능은 충분히 검증되지 않았다. 또한, 모델의 복잡도가 증가할 수 있으며, 계산 비용이 많이 소요될 수 있다는 단점이 있다. 향후 연구에서는 다양한 backbone 모델 및 특징을 탐색하고, 실제 환경에서의 성능 검증 및 효율성 개선을 위한 노력이 필요하다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
