# MultiAPI Spoof: A Multi-API Dataset and Local-Attention Network for Speech Anti-spoofing Detection

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2512.07352 · Xueping Zhang et al. · 2025

## 한 줄 요약

본 논문에서는 다양한 API에서 생성된 음성 데이터를 포함하는 MultiAPI Spoof 데이터셋을 소개하고, 이를 활용하여 API 출처 추적 태스크를 정의합니다. 또한, 로컬 어텐션 모듈을 강화한 Nes2Net-LA 모델을 제안하여 anti-spoofing 성능을 향상시켰으며, 다양한 벤치마크에서 최첨단 성능을 입증했습니다.

## 문제 정의

기존의 음성 anti-spoofing 벤치마크는 제한된 수의 공개 TTS 또는 VC 모델로 구성되어 있어 실제 환경의 다양한 API 사용 현황을 제대로 반영하지 못합니다. 특히, 산업계에서는 대부분 독점 또는 폐쇄형 API를 사용하므로 연구 모델이 실제 API 데이터에서 어떻게 작동할지 불확실합니다. 또한, 새로운 생성 모델의 등장으로 인해 연구 벤치마크와 실제 공격 간의 격차가 커지고 있습니다.

## 제안 방법

본 논문에서는 이러한 문제점을 해결하기 위해 MultiAPI Spoof 데이터셋을 구축하고, 이를 기반으로 API 출처 추적 태스크를 정의합니다. 또한, Nes2Net 아키텍처에 로컬 어텐션 모듈을 추가한 Nes2Net-LA 모델을 제안합니다. Nes2Net-LA는 Nested 블록 간의 교차 블록 상호작용을 강화하여 지역적 맥락 모델링 및 정밀한 spoofing 특징 추출을 개선합니다.

## 실험·결과

anti-spoofing 실험은 TIMIT, ODSS, FoR, AI4T, ASV5, MLAAD 등 6개의 공개 데이터셋을 사용하여 수행되었습니다. MultiAPI Spoof 데이터셋을 학습에 포함하면 모델의 cross-domain robustness가 향상되는 것을 확인했습니다. Nes2Net-LA 모델은 다양한 벤치마크에서 state-of-the-art 성능을 달성했으며, 특히 unseen spoofing 조건에서 높은 성능을 보였습니다. API tracing 태스크에서는 uanseen API에 대한 F1 점수가 높게 나타나 API 출처 추적의 가능성을 확인했습니다.

## 한계

본 연구에서는 다양한 API에서 생성된 음성 데이터를 사용했지만, 모든 API를 포괄하지는 못합니다. 또한, 데이터 증강 기법을 사용하지 않아 실제 환경에서의 robustness 측면에서 추가적인 개선이 필요할 수 있습니다. API tracing 태스크에서는 uanseen API에 대한 성능이 개선되었지만, 특정 API에 대해서는 여전히 낮은 성능을 보일 수 있습니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
