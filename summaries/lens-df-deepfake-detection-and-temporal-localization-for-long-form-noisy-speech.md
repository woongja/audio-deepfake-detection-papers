# LENS-DF: Deepfake Detection and Temporal Localization for Long-Form Noisy Speech

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2507.16220 · Xuechen Liu et al. · 2025

## ## 한 줄 요약

본 논문은 긴 지속 시간, 다양한 노이즈 조건, 다중 화자가 포함된 오디오를 생성하여 딥페이크 탐지 및 시간적 지역화 모델의 성능을 향상시키는 LENS-DF 데이터 생성 방법을 제안하고, 이를 통해 현실 세계의 복잡한 환경에서 더 robust한 딥페이크 탐지 성능을 입증한다.

## ## 문제 정의

기존의 딥페이크 탐지 모델은 주로 짧고 깨끗한 오디오 데이터로 훈련 및 평가되어 현실 세계의 복잡한 환경(긴 지속 시간, 노이즈, 다중 화자)에서 성능이 저하되는 경향이 있다. 이러한 현실적인 요인들을 제대로 반영하지 못한 훈련 및 평가 데이터셋의 한계는 딥페이크 탐지 모델의 실제 적용에 어려움을 야기한다. 따라서 현실 세계의 복잡한 조건을 포괄적으로 반영하는 훈련 및 평가 데이터셋과 방법을 개발하는 것이 시급하다.

## ## 제안 방법

본 연구에서는 긴 지속 시간, 다양한 노이즈 조건, 다중 화자가 포함된 오디오 샘플을 생성하는 LENS-DF(Longer duration, Enhanced multi-speaker, Noisy Speech for audio DeepFake detection) 데이터 생성 파이프라인을 제안한다. 이는 ASVspoof 2019 데이터셋을 기반으로 하며, 다양한 현실적인 요인들을 제어하여 데이터셋을 생성할 수 있도록 설계되었다. 생성된 데이터셋은 자기 지도 학습(SSL) 기반의 최신 오디오 딥페이크 탐지 모델을 훈련하고, 탐지 및 시간적 지역화 성능을 평가하는 데 사용된다. 또한, 다양한 현실적인 요인이 성능에 미치는 영향을 분석하기 위한 아블레이션 연구를 수행한다.

## ## 실험·결과

LENS-DF로 생성된 데이터셋으로 훈련된 모델은 기존의 짧고 깨끗한 오디오 데이터셋으로 훈련된 모델보다 탐지 및 시간적 지역화 성능에서 일관되게 더 나은 결과를 보였다. 특히, 긴 지속 시간 및 복잡한 조건에서 훈련된 모델은 성능 향상이 두드러졌다. 예를 들어, MMS-300M 모델은 LENS-DF 훈련 데이터셋에서 0.6%의 EER(Error Ejection Rate)를 달성했으며, 이는 기존의 19Laeval 데이터셋에서 0.15%의 EER보다 훨씬 낮은 수치이다. 또한, 시간적 지역화 성능 역시 LENS-DF 훈련 데이터셋을 사용한 모델에서 더 높은 성능을 보였다.

## ## 한계

본 연구는 LENS-DF 데이터 생성 방법을 통해 현실 세계의 복잡한 조건을 반영한 딥페이크 탐지 모델의 성능 향상을 입증했지만, 여전히 몇 가지 한계점을 가지고 있다. 첫째, LENS-DF는 ASVspoof 2019 데이터셋을 기반으로 생성되므로, 실제 다양한 환경의 모든 복잡한 요인을 완벽하게 포괄하지 못할 수 있다. 둘째, 본 연구에서는 자기 지도 학습(SSL) 기반의 모델을 주로 사용했으며, 다른 유형의 모델(예: 강화 학습 기반 모델)에 대한 성능은 추가적으로 연구가 필요하다. 셋째, 시간적 지역화 성능 향상에 대한 추가적인 연구를 통해 더욱 robust한 모델 개발이 가능할 것으로 예상된다.

## ## 6 Ablation Study

본 연구에서는 LENS-DF 데이터 생성 시 도입된 다양한 요인(노이즈 레벨, 지속 시간, 화자 수)이 탐지 및 지역화 성능에 미치는 영향을 분석하기 위한 아블레이션 연구를 수행했다.

## 6.4 Comparative results on LAV-DF

본 연구에서 제안한 LENS-DF 데이터셋으로 훈련된 모델의 성능은 기존의 오디오-비디오 딥페이크 탐지 데이터셋인 LAV-DF(Audio-Visual Deepfake)에서 훈련된 모델과 비교했을 때 AP(Average Precision) 측면에서 유사한 성능을 보였다. 이는 LAV-DF가 본 연구의 LENS-DF 데이터셋보다 현실 세계의 복잡한 환경을 더 잘 반영하고 있음에도 불구하고, LENS-DF 데이터셋으로 훈련된 모델이 유사한 성능을 달성했다는 것을 의미한다. 또한, LENS-DF 데이터셋으로 훈련된 모델은 LAV-DF 데이터셋으로 훈련된 모델보다 AR(Area Under the ROC curve) 측면에서 더 나은 성능을 보였으며, 이는 LENS-DF 데이터셋이 더 균형 잡힌 훈련 데이터셋을 제공했기 때문으로 해석될 수 있다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
