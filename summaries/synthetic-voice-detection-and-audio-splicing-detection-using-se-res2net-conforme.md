# Synthetic Voice Detection and Audio Splicing Detection using SE-Res2Net-Conformer Architecture

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2210.03581 · Lei Wang et al. · 2022

## 한 줄 요약

본 논문은 합성 음성 및 음성 splicing 탐지 성능 향상을 위해 SE-Res2Net 아키텍처에 Conformer 블록을 결합한 새로운 모델(SE-Res2Net-Conformer)을 제안하며, 음성 splicing 탐지 문제를 경계 탐지로 재정의하고 multi-classification 방식으로 접근한다.

## 문제 정의

인터넷 사용자 및 음성 인증과 같은 인공지능 시스템을 속이기 위해 합성 음성 및 음성 splicing 클립이 생성되고 있다. 기존 연구는 합성 음성 또는 음성 splicing 여부를 binary classification 문제로 다루었으나, 본 논문에서는 합성 음성 탐지 성능 향상과 음성 splicing 탐지 문제의 효율적인 해결을 목표로 한다. 특히, 음성 splicing 탐지 시 전체 splicing segment가 아닌 경계(boundary)를 탐지하는 것이 더 유용하며, 이를 위해 deep learning 기반 접근 방식을 제안한다.

## 제안 방법

본 논문에서는 합성 음성 탐지 및 음성 splicing 탐지라는 두 가지 문제를 해결하기 위해 SE-Res2Net 아키텍처에 Conformer 블록을 결합한 SE-Res2Net-Conformer 모델을 제안한다. SE-Res2Net은 이미지 처리 분야에서 multi-scale feature를 추출하는 데 효과적인 아키텍처이며, Conformer은 convolution과 self-attention 모듈을 결합하여 global temporal context와 local feature pattern을 모두 효과적으로 포착할 수 있다. SE-Res2Net에 Conformer 블록을 추가함으로써 합성 음성 및 음성 splicing의 특징을 더 잘 학습하고 분리할 수 있도록 한다. 또한, 합성 음성 탐지 문제를 binary classification이 아닌 TTS 및 VC로 분리하는 multi-classification 문제로 재정의하여 탐지 정확도를 높인다.

## 실험·결과

본 논문에서는 ASRspoof 2019 데이터셋을 사용하여 제안하는 SE-Res2Net-Conformer 모델의 성능을 평가했다. SE-Res2Net 모델은 clean 데이터에서 가장 낮은 SCA 값을 보였으며, 15dB 노이즈 데이터에서도 더 높은 SCA 값을 나타냈다. SE-Res2Net34-Conformer 모델은 모든 평가 세트에서 SCA가 약간 개선되었으나, 추가적인 데이터 증강을 통해 더 큰 성능 향상을 기대할 수 있다.

## 한계

본 연구는 ASRspoof 2019 데이터셋에 한정되어 있으며, 다양한 유형의 합성 음성 및 음성 splicing 공격에 대한 일반화 성능은 아직 검증되지 않았다. 또한, 음성 splicing 탐지 시 경계 탐지에 집중했기 때문에, splicing된 음성 segment의 내용 분석에는 한계가 있을 수 있다. 향후 연구에서는 더 다양한 데이터셋과 공격 유형에 대한 실험을 통해 모델의 일반화 성능을 높이고, splicing된 음성 segment의 내용 분석을 위한 추가적인 연구가 필요하다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
