# Audio Deepfake Detection in the Age of Advanced Text-to-Speech models

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2601.20510 · Robin Singh et al. · 2026

## 한 줄 요약

본 연구는 세 가지 최신 TTS 모델(Dia2, Maya1, MeloTTS)에 대해 네 가지 다양한 검출 프레임워크를 적용하여 성능을 비교 분석하고, 특히 UncovAI의 독점 모델이 모든 TTS 모델에 대해 높은 검출 성능을 보임을 확인했습니다.

## 문제 정의

텍스트 음성 변환(TTS) 기술의 발전은 합성 음성의 현실성을 크게 높였으며, 이에 따라 오디오 딥페이크 탐지에 새로운 도전 과제를 제시합니다. 기존의 딥페이크 탐지 방법은 주로 오래된 vocoder 기반 TTS 시스템의 특징적인 아티팩트에 의존해 왔으나, 최근 등장하는 새로운 아키텍처(LLM 기반, non-autoregressive)는 이러한 기존 방법으로는 탐지하기 어려울 수 있습니다. 따라서 다양한 TTS 아키텍처에 효과적으로 대응할 수 있는 새로운 탐지 전략이 필요합니다.

## 제안 방법

본 연구에서는 세 가지 대표적인 TTS 모델(Dia2, Maya1, MeloTTS)을 사용하여 12,000개의 합성 오디오 샘플을 생성하고, 이를 네 가지 다른 검출 프레임워크(semantic, structural, signal-level, foundation models)에 적용하여 성능을 비교 분석했습니다. 각 프레임워크는 음성 내용, 구조, 신호 특성 등 다양한 측면에서 오디오의 진위 여부를 판단합니다. 또한, 생성된 데이터셋의 품질을 평가하기 위해 intelligibility, speaker fidelity, acoustic realism, signal clarity, internal voice consistency 등 다양한 지표를 활용하여 각 모델의 특성을 분석했습니다.

## 실험·결과

실험 결과, 각 TTS 모델의 아키텍처에 따라 검출 프레임워크의 성능이 크게 달라지는 것을 확인했습니다. 특히 semantic detector는 비-autoregressive 모델인 MeloTTS에 강점을 보였으나, LLM 기반 모델인 Maya1에는 취약한 반면, structural detector는 autoregressive 모델인 Dia2에 효과적인 것으로 나타났습니다. 또한, UncovAI의 독점적인 오디오 딥페이크 검출 모델은 모든 TTS 모델에 대해 높은 검출 성능을 보여주었습니다.

## 한계

본 연구는 주로 합성 오디오 데이터셋을 사용하여 실험을 진행했으며, 실제 환경에서의 다양한 노이즈나 압축 아티팩트 등 외부 요인에 대한 robustness를 충분히 고려하지 못했습니다. 또한, 각 검출 프레임워크의 성능을 비교 분석했지만, 각 프레임워크의 장단점을 명확히 파악하고, 이를 결합한 하이브리드 방식에 대한 심층적인 연구는 부족합니다. 향후 연구에서는 실제 환경 데이터를 활용한 검증과 다양한 검출 프레임워크의 조합 및 최적화 연구가 필요합니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
