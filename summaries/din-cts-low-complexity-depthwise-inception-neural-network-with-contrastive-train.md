# DIN-CTS: Low-Complexity Depthwise-Inception Neural Network with Contrastive Training Strategy for Deepfake Speech Detection

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2502.20225 · Lam Pham et al. · 2025

## 한 줄 요약

본 논문에서는 저복잡도 Depthwise-Incption Network (DIN) 아키텍처와 Contrastive Training Strategy (CTS)를 결합하여 딥페이크 음성 검출(DSD) 성능을 높이는 새로운 모델을 제안하고, 실험적으로 우수한 성능을 입증했습니다.

## 문제 정의

딥러닝 기술 발전으로 인해 텍스트-음성 변환 등 다양한 분야에서 활용되는 음성 생성 시스템이 등장했지만, 동시에 악의적인 목적으로 제작된 딥페이크 음성 또한 생성될 수 있습니다. 이러한 딥페이크 음성은 보안 및 사회적 문제를 야기하므로, 딥페이크 음성을 정확하게 검출하는 기술(DSD)의 중요성이 부각되고 있습니다. 기존 DSD 시스템들은 복잡한 모델 구조나 많은 연산량을 요구하는 경우가 많아 실시간 적용에 어려움이 있습니다.

## 제안 방법

본 논문에서는 딥페이크 음성 검출을 위해 저복잡도 Depthwise-Incption Network (DIN) 아키텍처를 제안합니다. DIN은 depthwise convolution과 inception layer를 결합하여 특징 추출 능력을 향상시키고 모델의 복잡도를 줄입니다. 또한, bona fide와 fake 오디오 분포 간의 차이를 학습하는 Contrastive Training Strategy (CTS)를 적용하여 모델의 성능을 더욱 향상시킵니다. 제안된 모델은 Spectrogram 특징 추출, DIN 아키텍처, CTS의 세 가지 단계를 거쳐 딥페이크 음성을 검출합니다.

## 실험·결과

제안된 모델을 평가하기 위해 AVSspoof 2019 LA 데이터셋을 사용했습니다. 실험 결과, DIN 아키텍처를 기반으로 한 M1 모델이 ResNet18 기반의 baseline 모델보다 우수한 성능을 보였습니다. 특히, CTS를 적용한 M3 모델은 EER 4.6%, Accuracy 95.4%, F1 97.3%, AUC 98.9%의 뛰어난 성능을 달성했으며, 이는 AVSspoof 2019 LA 챌린지에서 제출된 단일 시스템 중 최고 수준의 결과입니다. 또한, 시각화 결과 DIN과 CTS의 결합이 bona fide와 fake 오디오 분포를 효과적으로 분리하는 데 기여함을 확인할 수 있었습니다.

## 한계

본 연구에서는 DIN 아키텍처와 CTS를 결합한 모델의 성능을 높이는 데 집중했으며, 다른 다양한 특징 추출 방법이나 학습 전략에 대한 탐색은 제한적으로 이루어졌습니다. 또한, 제안된 모델은 4초 길이의 오디오 세그먼트를 입력으로 사용하도록 설계되었으며, 더 긴 오디오 시퀀스에 대한 성능은 추가적인 연구가 필요합니다. 더불어, 실제 환경에서의 다양한 노이즈나 음성 변조에 대한 Robustness 평가가 부족하며, 실시간 시스템으로의 배포를 위한 최적화 작업 또한 추가적으로 고려해야 합니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
