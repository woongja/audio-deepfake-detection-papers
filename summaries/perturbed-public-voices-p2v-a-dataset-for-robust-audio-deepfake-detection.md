# Perturbed Public Voices (P2V): A Dataset for Robust Audio Deepfake Detection

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2508.10949 · Chongyang Gao et al. · 2025

## 한 줄 요약

본 논문은 실제 환경의 딥페이크 탐지 성능을 저해하는 요인들을 고려하여 구축된 대규모 데이터셋 P2V를 소개하고, 기존 탐지 모델의 취약성을 입증하며, 실제 환경에서의 강력한 탐지를 위한 학습의 중요성을 강조합니다.

## 문제 정의

기존의 오디오 딥페이크 탐지 모델은 통제된 환경에서 높은 성능을 보이지만, 실제 환경에서는 신뢰할 수 없는 성능을 보입니다. 이는 기존 데이터셋이 실제 환경의 복잡성(환경 노이즈, 적대적 교란, 실제와 유사한 콘텐츠)을 충분히 반영하지 못하기 때문입니다. 이러한 불일치는 실제 환경에서 딥페이크 탐지 모델의 신뢰성을 떨어뜨리는 주요 원인입니다.

## 제안 방법

본 논문에서는 실제 환경의 딥페이크 탐지 성능을 향상시키기 위해 Perperturbed Public Voices (P2V)라는 새로운 대규모 데이터셋을 제안합니다. P2V 데이터셋은 206명의 사망한 유명인의 오디오 샘플을 기반으로 하며, 다음과 같은 특징을 가집니다. 첫째, 3가지 다른 LLM을 사용하여 생성된 대본을 통해 실제 대화와 유사한 맥락성을 확보합니다. 둘째, 10가지의 다양한 환경 노이즈와 적대적 교란을 적용하여 실제 환경의 복잡성을 반영합니다. 셋째, 2020년부터 2025년까지의 10가지 최첨단 음성 복제 및 변환 기술을 사용하여 다양한 합성 오디오를 포함합니다.

## 실험·결과

본 연구에서는 22개의 최첨단 오디오 딥페이크 탐지 모델을 사용하여 P2V 데이터셋과 기존 벤치마크 데이터셋(In-The-Wild)에서 실험을 수행했습니다. 실험 결과, P2V 데이터셋에서 학습된 모델은 기존 데이터셋에서 학습된 모델보다 43%의 성능 저하를 보였지만, 실제 환경 데이터에서 더 높은 일반화 성능을 나타냈습니다. 특히, 간단한 환경 노이즈나 적대적 교란은 최대 16%의 성능 저하를 일으켰으며, 최신 음성 복제 기술은 탐지 가능성을 20~30% 감소시키는 것으로 나타났습니다. 이러한 결과는 기존 탐지 모델이 실제 환경의 복잡성에 취약하며, 실제 환경에서의 강력한 탐지를 위해서는 실제 환경과 유사한 데이터로 학습하는 것이 중요함을 시사합니다.

## 한계

본 연구는 실제 환경의 딥페이크 탐지 성능 향상을 위한 중요한 단계를 제시하지만, 데이터셋 구축 및 모델 학습 과정에서 발생할 수 있는 모든 잠재적인 편향이나 한계를 완전히 해결하지 못했을 수 있습니다. 또한, 다양한 유형의 딥페이크 공격(예: 음성 변조, 합성 음성)에 대한 탐지 성능을 보다 심층적으로 분석하고, 실제 환경에서의 다양한 조건(예: 다양한 마이크, 다양한 오디오 환경)에 대한 강건성을 평가하는 추가적인 연구가 필요합니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
