# RawTFNet: A Lightweight CNN Architecture for Speech Anti-Spoofing

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2507.08227 · Yang Xiao et al. · 2025

## 한 줄 요약

본 논문에서는 시간 및 주파수 정보를 분리하여 처리하는 경량 CNN 아키텍처인 RawTFNet을 제안한다.

## 문제 정의

자동 음성 인식(ASV) 시스템은 피싱 공격에 취약하며, 특히 최근 자연스러운 인공 음성 생성 기술의 발전으로 인해 더욱 심각한 위협에 직면하고 있다. 기존의 대규모 모델들은 높은 컴퓨팅 자원을 요구하며, 실용적인 시스템 구축에 어려움이 있다. 따라서, 피싱 공격을 효과적으로 탐지하면서도 효율적인 음성 처리 방법을 찾는 것이 중요하다.

## 제안 방법

본 논문에서는 시간 및 주파수 정보를 분리하여 처리하는 RawTFNet이라는 새로운 피싱 탐지 모델을 제안한다. RawTFNet은 SiNC 필터를 통해 raw 음성 신호에서 특징을 추출하고, DWs SE-Res2Net 블록을 사용하여 특징의 다양성과 채널 주의를 강화한다. 핵심적으로는 TF-Convs 모듈을 도입하여 시간 및 주파수 차원을 분리하여 처리함으로써 피싱 공격의 미세한 특징을 효과적으로 포착한다.

## 실험·결과

RawTFNet은 ASVspoof 2021 LA 및 DF 데이터셋에서 기존 SOTA 모델과 비교 가능한 정확도를 달성했다. 특히, 70K 파라미터만을 사용하여 모델 크기를 크게 줄이고, 연산량 또한 감소시켰다. Ablation study 결과, 시간 및 주파수 분리 처리가 피싱 탐지에 중요한 역할을 하는 것으로 확인되었다.

## 한계

본 연구에서는 시간 및 주파수 정보를 분리하여 처리하는 방식에 집중했으며, 다른 유형의 특징 표현이나 모델 구조를 탐색하지는 않았다. 또한, 제안된 모델이 모든 종류의 피싱 공격에 대해 동일한 성능을 보장하는지는 추가적인 검증이 필요하다.

## 2 Proposed RawTFNet

피싱 공격에 대한 기존 경량 음성 인식 모델의 한계를 극복하기 위해, 시간 및 주파수 정보를 명시적으로 분리하여 처리하는 피싱 탐지 모델인 RawTFNet을 제안한다. RawTFNet은 음성 신호의 시간적 및 주파수적 특성을 더 잘 포착하기 위해 차원 인식 처리를 설계하는 데 영감을 받았다. 그림 [1] (a)는 제안된 RawTFNet의 전체 구조를 보여준다. 특히, 시간 및 주파수 처리를 분리하는 새로운 TF-Convs 모듈을 도입했으며, 낮은 연산량으로도 특징 다양성과 채널 주의를 향상시키는 DWs SE-Res2Net 블록을 통합했다.

## 2.1 Frontend

RawTFNet의 프론트엔드는 raw 음성 신호에서 저수준 스펙트로-템포럴 특징을 직접 추출하도록 설계되었다. 이를 위해 RawNet2의 수정된 버전을 채택하여, 70개의 필터를 가진 학습 가능한 SiNC 필터를 통과시켜 신호를 주파수 및 시간 밴드 특징 맵으로 변환한다.

다음으로 추출된 특징은 일련의 컨볼루션 레이어를 통해 정제된다. 결과적으로 생성된 출력은 초기 패턴을 포착하는 32개의 필터를 가진 ResNet 블록을 통과한다. 이후, 다수의 수용 영역을 활용하여 특징 다양성을 향상시키는 64개의 필터를 가진 세 개의 SE-Res2Net 블록을 처리한다. 효율성을 더욱 높이기 위해 표준 2D 컨볼루션을 DWs 컨볼루션으로 대체한다. 이는 그림 [1] (b)에서 볼 수 있듯이 연산량을 줄이면서도 성능을 유지하는 DWs SE-Res2Net 블록의 사용으로 이어진다. 각 블록에는 채널 주의를 강화하는 squeeze-and-excitation (SE) 모듈도 포함된다. 그 결과, 인코더는 CC, FF, TT가 각각 채널 수, 주파수 밴드 수, 시간 단계 수를 나타내는 고수준 특징 맵 xHFM∈ℝC×F×T를 생성한다.

## 3 Experimental Setup

ASVspoof 2021 LA 및 DF 데이터셋을 사용하여 RawTFNet의 성능을 평가했다. 실험 설정은 다음과 같다. (구체적인 설정은 논문에 명시되어 있음)

## 4 Results and Discussions



## 4.3 Ablation Study

RawTFNet-32의 각 구성 요소의 영향을 분석하기 위해 ASVspoof 2021 LA 세트에서 ablation study를 수행한 결과를 표 II에서 확인할 수 있다. 주파수 또는 시간 브랜치를 제거하면 EER이 각각 6.91% 및 7.08%로 크게 증가하여 시간 및 주파수 정보를 모두 모델링하는 것의 중요성을 확인했다. shuffle 연산을 제외하면 모델 크기나 비용에 영향을 주지 않으면서 성능이 약간 감소하는 것으로 나타났다(EER: 5.74%), 이는 특징 상호 작용에서 shuffle 연산의 긍정적인 역할을 보여준다. 이러한 결과는 모든 구성 요소가 RawTFNet의 효과 및 효율성에 기여함을 입증한다.

## 5 Conclusions

본 연구에서는 시간 및 주파수 정보를 분리하여 처리하는 경량 효과적인 피싱 탐지 모델인 RawTFNet을 소개한다. TF-Convs를 통해 시간 및 주파수 특징 처리를 분리하고, DWs SE-Res2Blocks 및 유연한 채널 너비 설계를 통해 낮은 연산 비용으로도 상세한 정보를 포착한다. ASVspoof 2021 LA 및 DF 평가 데이터셋에 대한 실험 결과는 RawTFNet이 기존 SOTA 시스템과 비교 가능한 정확도를 가지면서도 더 적은 파라미터와 낮은 MACs를 달성함을 보여준다. ablation study 결과는 특히 시간-주파수 경로의 중요성을 확인한다. 요약하자면, RawTFNet은 실세계 피싱 탐지 시나리오에 적합한 강력하고 효율적인 프레임워크를 제공한다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
