# Anchoring the Unknown: Open-Set Model Attribution via Proxy-Anchor Learning

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2606.10758 · Cristian-Teodor Neamtu et al. · 2026

## 한 줄 요약

본 논문에서는 새로운 모델 어키텍처 묶기 전략과 Proxy-Anchor metric learning을 결합하여 텍스트 음성 변환(TTS) 시스템의 출처 추적 및 알려지지 않은 시스템 감지에 효과적인 프레임워크를 제시합니다.

## 문제 정의

텍스트 음성 변환(TTS) 기술의 발전으로 인해 생성된 합성 음성을 실제 음성과 구별하기 어려워지고 있으며, 특히 알려지지 않은 TTS 시스템에서 생성된 음성을 식별하는 출처 추적(source tracing)은 여전히 해결해야 할 과제로 남아있습니다. 기존의 폐쇄 집합(closed-set) 방식은 알려진 시스템에만 적용 가능하며, 개방 집합(open-set) 방식은 제한된 수의 시스템으로 평가되는 경우가 많습니다. 또한, 동일한 아키텍처의 다양한 버전들이 존재하여 모델 아키텍처에 따른 혼동이 발생할 수 있습니다.

## 제안 방법

본 연구에서는 Proxy-Anchor 손실 함수를 기반으로 하는 새로운 metric learning 프레임워크를 제안합니다. 이 프레임워크는 Wav2Vec2-BERT 임베딩을 활용하여 TTS 출처를 식별하고 알려지지 않은 시스템의 음성을 감지하는 데 사용됩니다. 또한, 모델 아키텍처를 묶는 전략을 도입하여 학습 데이터 내의 유사한 시스템들을 하나의 클래스로 묶고, 이를 통해 모델의 일반화 성능을 향상시킵니다.

## 실험·결과

제안한 프레임워크는 MLAAD v9 데이터셋에서 다양한 실험을 통해 평가되었습니다. 폐쇄 집합(closed-set) 평가에서는 99.76%의 정확도를 달성했으며, 알려지지 않은 시스템 감지(OOD detection)에서는 2.04%의 False Positive Rate@95를 기록했습니다. 이는 기존의 최첨단 방법 대비 OOD 정확도를 거의 두 배로 향상시키고, FPR@95를 크게 감소시킨 결과입니다. 특히, 모델 아키텍처를 묶는 전략을 적용한 결과, FPR@95가 4.7배 감소하는 효과를 보였습니다.

## 한계

본 연구는 주로 텍스트 음성 변환 시스템의 출처 추적 및 알려지지 않은 시스템 감지에 초점을 맞추고 있으며, 실제 환경에서의 견고성 및 적대적 공격에 대한 연구는 아직 부족합니다. 또한, 새로운 TTS 아키텍처에 대한 적은 데이터(few-shot adaptation)에 대한 연구도 향후 과제로 남습니다.

## 실험 세부 내용



## 평가 지표

제안한 프레임워크의 성능을 평가하기 위해 두 가지 주요 지표를 사용했습니다. 첫째, 알려진 시스템에 대한 모델 출처 매핑 정확도(Model attribution)를 평가하기 위해 분류 정확도를 사용했습니다. 둘째, 알려지지 않은 시스템의 음성을 정확하게 감지하는 능력을 평가하기 위해 Area Under the Receiver Operating Characteristic Curve (AUROC), precision, F1-score, 그리고 True Positive Rate (TPR)에서 False Positive Rate (FPR)@95를 사용했습니다.

## 실험 설정

실험에서는 Wav2Vec2-BERT 모델의 4번째 레이어 임베딩을 사용했으며, Proxy-Anchor 손실 함수를 통해 모델 파라미터와 각 TTS 시스템의 프로토타입 임베딩을 학습했습니다. 모델 아키텍처를 묶는 전략은 MLAAD v9 데이터셋의 110개 아키텍처 버전을 10개의 그룹으로 묶는 방식으로 적용되었습니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
