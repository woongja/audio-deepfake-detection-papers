# Probing-Guided Layer Selection from Self-Supervised Speech Models for Generalizable Audio Deepfake Detection

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2606.30791 · Marjan Beheshti et al. · 2026

## 한 줄 요약

본 논문은 self-supervised speech 모델의 레이어 선택을 위해 XGBoost probing 기법을 제안합니다. 이를 통해 각 모델에 적합한 레이어 조합을 찾아 cross-domain 음성 디페이크 탐지의 성능을 크게 향상시킬 수 있음을 실험적으로 입증했습니다.

## 문제 정의

음성 디페이크 탐지 시스템은 종종 특정 공격 방식이나 녹음 조건에 의존하여 다양한 환경에서 일반화하는 데 어려움을 겪습니다. self-supervised speech 모델은 다양한 깊이의 풍부한 표현을 제공하지만, 기존 방법들은 단일 레이어를 사용하거나 모든 레이어를 섞어 사용하거나, 학습 후 레이어 중요도를 파악하는 방식에 그쳐 cross-domain 일반화 문제를 해결하기에 충분하지 않습니다.

## 제안 방법

본 논문에서는 모델에 독립적인 두 단계의 probing 기반 레이어 선택 방법을 제안합니다. 첫 번째 단계에서는 lightweight XGBoost probing을 사용하여 각 Transformer 레이어의 cross-domain discriminative power를 평가하고 레이어 순위를 매깁니다. 두 번째 단계에서는 상위 순위 레이어들을 선택하여 per-layer attention pooling과 shared bottleneck projection을 통해 새로운 신경망 분류기를 학습시킵니다.

## 실험·결과

XLS-R-300M 모델에 제안된 방법을 적용한 결과, 상위 4개의 레이어(6, 7, 17, 19)를 선택했을 때 In-The-Wild 데이터셋에서 4.94±0.32%의 동일 오류율을 달성했습니다. 이는 기존의 frozen backbone 방식 대비 28%의 성능 향상입니다. 또한, WavLM Large 및 XLSR-53 모델에 대해서도 유사한 probing 기반 레이어 선택이 효과적임을 확인했습니다.

## 한계

본 연구는 cross-domain 일반화 성능 향상에 초점을 맞추었으며, 특정 공격 방식이나 녹음 조건에 대한 추가적인 분석은 포함하지 않았습니다. 또한, 제안된 방법이 모든 모델 아키텍처에 대해 최적의 레이어 조합을 보장하는 것은 아닙니다.

---

## 5.1 XLS-R-300M Layer Pprobing Results

XLS-R-300M 모델에 probing을 적용한 결과, cross-domain balanced accuracy 순위는 레이어 19(79.0%), 6(77.7%), 17(76.5%), 7(76.4%) 순으로 나타났습니다. 이는 mid-depth 레이어(6, 7)와 late-depth 레이어(17, 19)를 포함하는 영역에 정보가 집중되어 있음을 시사합니다.

![Refer to caption](/html/2606.30791/assets/Figure_2_heatmap.png)

**Table 3: Pprobing-guided layer selection across three backbones.**

| Bacbackone | Pretraining | Top-4 | Zone |
| --- | --- | --- | --- |
| XLS-R-300M | 436k hrs, 128 langs contrasive | {6,7,17,19} | Mid + Late |
| WavLM Large | 94k hrs, English contrasive + denoising | {7,8,9,10} | Mid only |
| XLSR-53 | 56k hrs, 53 langs contrasive | {14,17,20,21} | Late only |

XLS-R-300M은 mid-depth 레이어와 late-depth 레이어 모두에서 높은 성능을 보였으며, WavLM Large는 mid-depth 레이어에만 집중되는 경향을 보였습니다. XLSR-53은 late-depth 레이어에 더 높은 가중치를 부여했습니다. 이는 각 모델의 pretraining 데이터와 목표에 따라 정보가 저장되는 레이어 깊이가 다를 수 있음을 보여줍니다. 또한, XLS-R-300M에서 선택된 레이어 조합을 WavLM Large에 적용하면 성능이 저하되는 것으로 확인되어, 모델별 probing의 중요성을 강조합니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
