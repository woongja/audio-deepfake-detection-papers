# Defense for Black-box Attacks on Anti-spoofing Models by Self-Supervised Learning

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2006.03214 · Haibin Wu et al. · 2020

## 한 줄 요약

본 논문에서는 self-supervised learning 모델을 활용하여 black-box 공격으로부터 anti-spoofing 모델을 보호하는 방어 기법을 제안하고, 실험적으로 그 효과를 입증합니다.

## 문제 정의

자동 음성 인식(ASV) 시스템은 화자 확인에 널리 사용되지만, 텍스트-음성 변환, 음성 변환, 음성 재구축 등의 방식으로 생성된 스포오핑 오디오에 취약합니다. 이러한 스포오핑 오디오는 공격자가 특정 사용자를 모방하여 ASV 시스템을 속이는 데 사용될 수 있습니다. 따라서 스포오핑 오디오를 탐지하고 필터링하는 anti-spoofing 모델이 필요합니다.

## 제안 방법

본 논문에서는 self-supervised learning 모델인 Mockinjay를 활용하여 anti-spoofing 모델을 보호하는 방어 기법을 제안합니다. Mockinjay는 레이블이 없는 대량의 음성 데이터로부터 high-level하고 컨텍스트화된 표현을 학습합니다. 이러한 표현은 스포오핑 공격에 대한 방어력을 높이는 데 활용될 수 있습니다. 또한, deep 모델이 adversarial noise를 효과적으로 방어하는 정도를 정량화하기 위해 Layer-wise Noise to Signal Ratio (LNSR)를 제안합니다.

## 실험·결과

ASVspoof 2019 데이터셋을 사용하여 실험을 진행했습니다. Mockinjay 모델은 Librispeech 데이터셋으로 사전 훈련되었으며, LCNN 및 SENet과 같은 고성능 anti-spoofing 모델의 high-level representation 추출에 사용되었습니다. 공격 모델로 사용된 LCNN 및 SENet에 FGSM 및 PGD 공격을 가하고, 다양한 섭동 크기(ϵ)에 대해 방어 성능을 평가했습니다. 실험 결과, Mockinjay 모델이 생성한 high-level representation은 adversarial examples의 전이성을 효과적으로 방지하고 black-box 공격을 성공적으로 방어하는 것으로 나타났습니다. 또한, LNSR 분석을 통해 self-supervised learning 모델이 adversarial noise를 layer별로 완화시키는 것을 확인했습니다.

## 한계

본 연구는 self-supervised learning 모델을 활용한 anti-spoofing 모델 방어에 초점을 맞추고 있으며, 다양한 공격 기법 및 모델에 대한 일반화 가능성은 아직 탐색되지 않았습니다. 또한, self-supervised learning 모델의 학습 과정 및 파라미터 튜닝에 대한 추가적인 연구가 필요합니다.

## 4 Experiment



## 4.1 Experiment setup

본 실험에서는 AVSspoof 2019 challenge의 LA 파티션을 사용했습니다. LA 파티션은 텍스트-음성 변환 및 음성 변환으로 생성된 스포오핑 오디오를 포함하며, 훈련, 개발, 평가 세트로 나뉩니다.

Mockinjay 모델은 LARGE 모델의 기존 프레임워크를 따르며, 12개의 Transformer Encoder 레이어로 구성됩니다. 사전 훈련은 Librispeech 데이터셋의 360시간 음성 데이터를 사용하여 수행했습니다. 공격 모델로는 LCNN 및 SENet을 사용했으며, 이들의 기본 모델은 mel-spectrogram을 기반으로 훈련되었습니다. black-box 공격 시나리오에서는 기본 LCNN 및 SENet을 공격 모델로 사용하고, Figure [2]에 제시된 모델을 타겟 모델로 사용했습니다. FGSM 및 PGD 공격 알고리즘을 사용했으며, ϵ 값은 0.1, 1, 2, 4, 8, 16의 값을 가집니다.

## 4.2 Result and analysis



## 5 Conclusion

본 연구에서는 self-supervised learning 모델을 활용하여 black-box 공격으로부터 anti-spoofing 모델을 보호하는 방어 기법을 제안했습니다. 실험 결과, self-supervised learning 모델이 추출한 high-level representation은 adversarial examples의 전이성을 방지하고 black-box 공격을 성공적으로 방어하는 것으로 나타났습니다. 또한, Layer-wise Noise to Signal Ratio (LNSR)를 통해 self-supervised learning 모델이 adversarial noise를 layer별로 완화시키는 것을 확인했습니다. 향후 연구에서는 다양한 학습 목표를 가진 self-supervised learning 모델의 방어 능력과 더 많은 음성 처리 응용 분야에 대한 적용 가능성을 탐색할 계획입니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
