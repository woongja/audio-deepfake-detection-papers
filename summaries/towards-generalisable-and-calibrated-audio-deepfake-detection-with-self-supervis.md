# Towards generalisable and calibrated audio deepfake detection with self-supervised representations

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2309.05384 · Dan Oneaţă et al. · 2023

## 한 줄 요약

본 연구에서는 audio deepfake 탐지에 있어 일반화 능력과 교정(calibration)의 중요성을 강조하고, 사전 학습된 self-supervised representation과 로지스틱 회귀 분류기를 결합하는 간단하면서도 효과적인 방법을 제안합니다.

## 문제 정의

audio deepfake는 최근 합성 기술의 발전으로 인해 악의적인 목적으로 사용될 가능성이 높아지고 있습니다. 따라서 실제 오디오와 가짜 오디오를 자동으로 구별하는 audio deepfake 탐지 기술의 발전이 중요합니다. 기존의 audio deepfake 탐지 모델들은 일반화 능력에서 부족한 모습을 보이며, 특히 실제 환경과 다른 데이터셋에 대해서는 성능이 저하되는 경향이 있습니다. 또한, 탐지 모델의 예측 결과가 신뢰할 수 있는지(교정)에 대한 연구는 미흡한 실정입니다.

## 제안 방법

본 연구에서는 사전 학습된 self-supervised representation을 활용하여 audio deepfake 탐지 모델의 일반화 능력과 교정 성능을 향상시키는 방법을 제안합니다. 구체적으로, wav2vec 2.0 계열의 대규모 사전 학습된 representation을 고정하고, 이를 입력으로 받아 로지스틱 회귀 분류기를 학습시키는 방식을 사용합니다. 이러한 접근 방식은 복잡한 모델을 사용하는 대신, representation의 잠재적인 일반화 능력과 교정 효과를 활용합니다.

## 실험·결과

본 연구에서는 8개의 다양한 audio deepfake 데이터셋을 포함하는 벤치마크에서 제안하는 방법이 기존 모델 대비 우수한 성능을 보였습니다. 특히, ASRvspoof’19 데이터셋에서 equal error rate (EER)를 크게 감소시켰으며, 다양한 크기의 wav2vec 2.0 모델과 로지스틱 회귀 분류기를 사용하여 실험한 결과, 대규모 모델과 낮은 정규화 강도를 가진 로지스틱 회귀 분류기가 좋은 성능을 나타냈습니다.

## 한계

본 연구는 제안하는 방법이 다양한 데이터셋에서 좋은 성능을 보였지만, 왜 특정 데이터셋에서 성능이 일관되지 않게 나타나는지에 대한 근본적인 원인을 명확하게 설명하지 못한다는 한계가 있습니다. 이는 audio deepfake 탐지 기술 전반에 대한 미해결 과제이며, 향후 연구에서 더 깊이 있는 분석이 필요합니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
