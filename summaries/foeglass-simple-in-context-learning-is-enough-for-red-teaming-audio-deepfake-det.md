# FoeGlass: Simple In-Context Learning Is Enough for Red Teaming Audio Deepfake Detectors

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2606.05101 · Sepehr Dehdashtian et al. · 2026

## 한 줄 요약

본 논문은 텍스트-음성 변환(TTS) 모델을 활용하여 오디오 딥페이크 탐지기(ADD)의 취약점을 효과적으로 찾아내는 새로운 자동화된 레드팀िंग 방법인 FoeGlass를 제안합니다.

## 문제 정의

오디오 딥페이크 탐지 모델은 악의적인 목적으로 사용될 수 있는 텍스트-음성 변환 모델로부터 생성된 오디오를 식별하는 데 필수적입니다. 그러나 기존의 딥페이크 탐지 모델 평가 방법은 생성된 오디오의 다양한 오류 영역을 충분히 탐색하지 못하며, 특히 단일 TTS 모델에서 발생할 수 있는 복잡하고 예측 불가능한 오류 패턴을 간과하는 경향이 있습니다. 이러한 한계를 극복하기 위해 ADD 모델의 취약점을 효과적으로 찾아낼 수 있는 자동화된 레드팀링 방법이 필요합니다.

## 제안 방법

본 논문에서는 텍스트-음성 변환 모델의 입력 공간을 탐색하여 ADD 모델을 속이는 오디오 샘플을 생성하는 자동화된 레드팀링 방법인 FoeGlass를 제안합니다. 딥러닝 기반의 대규모 언어 모델(LLM)을 활용하여 ADD 모델의 실현 가능성 점수와 생성된 오디오의 다양성 점수를 조건으로 입력 샘플을 생성하고, 이를 통해 ADD 모델의 오류 가능성이 높은 영역을 탐색합니다. 특히, 다양성 점수를 활용하여 공격 과정에서 발생할 수 있는 모드 붕괴 문제를 완화하고, 공격 성공률을 높입니다.

## 실험·결과

실험에서는 다양한 오픈 소스 ADD 모델과 TTS 모델을 사용하여 FoeGlass의 성능을 평가했습니다. FoeGlass는 비조건부 샘플링에 비해 오디오 딥페이크 탐지에서 상당한 성능 향상(최대 94%의 FNR 감소)을 보였으며, 기존의 스푸핑 데이터셋으로는 탐지되지 않던 새로운 유형의 오류를 식별하는 데 성공했습니다. 또한, FoeGlass로 생성된 공격 샘플은 다양한 ADD 모델에 대해 전송 가능성을 보였으며, ADD 모델을 훈련하는 데 활용했을 때 모델의 견고성을 크게 향상시키는 것으로 나타났습니다.

## 한계

FoeGlass는 공격 대상 ADD 모델과 TTS 모델에 대한 블랙박스 접근 방식이므로, 모델 내부 구조에 대한 정보 없이 공격 샘플을 생성합니다. 또한, 공격 성공률은 딥러닝 기반의 공격 LLM의 성능에 의존하며, 특정 유형의 ADD 모델이나 TTS 모델에 대해서는 효과가 제한될 수 있습니다. 더 나아가, 텍스트 프롬프트의 품질에 따라 공격 성공률이 달라질 수 있으며, 최적의 프롬프트를 찾기 위한 추가적인 노력이 필요할 수 있습니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
