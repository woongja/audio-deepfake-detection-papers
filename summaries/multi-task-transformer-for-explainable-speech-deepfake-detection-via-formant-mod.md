# Multi-Task Transformer for Explainable Speech Deepfake Detection via Formant Modeling

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2601.14850 · Viola Negroni et al. · 2026

## 한 줄 요약

본 논문에서는 음성 딥페이크 감지를 위한 새로운 경량화된 멀티태스크 트랜스포머 모델 SFATNet-4를 제안한다. 이 모델은 음성 패턴 예측, 음성/비음성 영역 구분, 딥페이크 감지를 동시에 수행하며, 시간 분할 기반의 입력 처리와 attention 메커니즘을 통해 높은 해석 가능성을 제공한다.

## 문제 정의

음성 딥페이크는 사회적 혼란과 악의적인 목적으로 사용될 가능성이 높아 심각한 위험을 초래한다. 기존의 음성 딥페이크 탐지 모델들은 성능은 우수하지만, 모델의 의사 결정 과정에 대한 해석이 어렵다는 한계가 있다. 특히, 모델이 어떤 음성 특징에 의존하여 딥페이크 여부를 판단하는지 파악하기 어렵다는 점이 문제로 지적된다.

## 제안 방법

본 논문에서는 기존의 SFATNet-3 모델을 기반으로, 효율성, 계산량 감소, 해석 가능성 향상을 목표로 하는 SFATNet-4를 제안한다. SFATNet-4는 두 개의 인코더(진폭 및 위상 처리), 세 개의 디코더(음성 패턴 예측, 음성/비음성 영역 구분, 딥페이크 감지)로 구성된 멀티태스크 모델이다. 특히, 입력 음성 신호를 시간 분할하여 처리하고, 음성 패턴 예측 및 음성/비음성 영역 구분이라는 추가적인 학습 목표를 도입하여 모델의 해석 가능성을 높였다.

## 실험·결과

실험 결과, 제안된 SFATNet-4 모델은 기존 모델 대비 더 적은 파라미터를 사용하고 학습 속도가 빠르며, 음성 패턴 예측 및 음성/비음성 영역 구분 능력이 향상되었다. 특히, 시간 분할 기반의 입력 처리와 attention 메커니즘을 통해 모델의 의사 결정 과정에 대한 해석이 용이해졌다. 음성 신호의 진폭 및 위상 정보를 동시에 처리하고, 음성/비음성 영역을 구분하는 능력은 딥페이크 감지 성능 향상에 기여했다.

## 한계

제안된 모델은 시간 분할 기반의 입력 처리를 사용하므로, 입력 신호의 시간적 정보 손실 가능성이 존재한다. 또한, 현재 실험은 특정 데이터셋(ASVspoof 5)에 초점을 맞추고 있으며, 다양한 도메인의 음성 데이터에 대한 일반화 성능은 추가적인 검증이 필요하다. 더불어, 모델의 해석 가능성이 향상되었지만, 여전히 모델의 모든 의사 결정 과정을 완벽하게 설명하는 것은 어려운 점이 있다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
