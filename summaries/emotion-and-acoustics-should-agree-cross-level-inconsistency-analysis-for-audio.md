# Emotion and Acoustics Should Agree: Cross-Level Inconsistency Analysis for Audio Deepfake Detection

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2601.13847 · Jinhua Zhang et al. · 2026

## 한 줄 요약

본 논문에서는 감정 및 음향 특징 간의 불일치를 주요 탐지 신호로 활용하는 새로운 ADD 프레임워크 EAI-ADD를 제안하며, 다양한 실험 결과에서 기존 방법들을 능가하는 성능을 입증했습니다.

## 문제 정의

기존의 음성 위조 탐지(ADD) 방법들은 음향 및 감정 특징 간의 상관관계를 활용하는 경향이 있지만, 실제 음성에서는 감정 역학이 시간적으로 부드럽게 변화하며 음향 구조와 긴밀하게 연관되어 있습니다. 따라서 기존 방법들은 이러한 미묘한 불일치를 놓치거나 통계적 노이즈로 간주하는 한계가 있습니다.

## 제안 방법

본 논문에서는 감정-음향 불일치를 주요 탐지 신호로 활용하는 EAI-ADD 프레임워크를 제안합니다. 이 프레임워크는 감정 및 음향 특징을 통합된 공간으로 투영하는 감정-음향 일치성 매핑 모듈(EAAM)과, 감정-음향 불일치를 모델링하는 감정-음향 불일치 모델링 모듈(EAIMM)로 구성됩니다. EAAM은 감정 및 음향 특징의 시간적, 척도적 변화를 포착하고, EAIMM은 이를 기반으로 감정-음향 불일치 패턴을 식별합니다.

## 실험·결과

제안된 EAI-ADD 방법은 AVSspoof 2019LA 및 2021LA 데이터셋에서 기존의 강력한 baseline 모델들을 능가하는 성능을 보였습니다. 특히, t-DCF(최소 0.0110) 및 EER(최소 0.34%)에서 우수한 결과를 나타냈으며, 이는 감정-음향 불일치 모델링의 효과를 입증합니다. 또한, 2021LA 데이터셋에서도 일관된 성능 향상을 보이며, 다양한 위조 공격 조건에서도 강력한 탐지 능력을 유지했습니다.

## 한계

현재까지 실시간 또는 고압축 조건에서의 실제 환경에서의 검증은 이루어지지 않았으며, 향후 연구의 중요한 방향으로 제시됩니다.

## 1 Introduction

음성 위조 탐지(ADD)는 진실한 음성에서 위조된 음성을 식별하는 것을 목표로 하며, 최근 많은 관심을 받고 있습니다. 기존 연구들은 종종 강한 음향 및 감정 특징 간의 상관관계가 진실한 음성을 나타낸다고 가정하고, 이러한 상관관계를 강화하거나 측정하는 데 집중합니다. 그러나 기존 방법들은 감정 및 음향 특징을 분리하여 분석하거나, 상관관계 지표에 의존하는 경향이 있으며, 이는 감정과 음향 사이의 미묘한 동기화 불일치를 간과하거나 단순화할 수 있습니다. 예를 들어, 감정적 흥분의 점진적인 증가와는 일관된 음향 변화가 동반되는 반면, 합성 음성은 프로소-감정 동기화 불일치, 즉 갑작스러운 음향 변동과 일치하지 않는 감정 변화를 보이는 경우가 많습니다. 이러한 감정-음향 불일치는 위조 음성의 중요한 특징이 될 수 있습니다.

본 논문에서는 감정-음향 불일치를 ADD의 핵심 신호로 활용하는 새로운 EAI-ADD 프레임워크를 제안합니다. EAI-ADD는 감정-음향 일치성 매핑 모듈(EAAM)을 통해 감정 및 음향 특징을 통합된 공간으로 투영하고, 감정-음향 불일치 모델링 모듈(EAIMM)을 통해 시간적, 척도적 다양한 불일치를 분석하여 위조 음성 탐지에 활용합니다.

## 2 EAI-ADD Neural Architecture

본 논문에서는 EAI-ADD(그림 2)를 제안합니다. EAI-ADD는 특징 추출 과정, 감정-음향 일치성 매핑 모듈(EAAM), 감정-음향 불일치 모델링 모듈(EAIMM), 그리고 이진 분류기로 구성됩니다. 특징 추출 과정은 프레임 및 발화 수준의 감정 특징과 프레임 수준의 음향 특징을 생성합니다. EAAM은 감정 및 음향 특징을 통합된 표현 공간으로 투영하고, EAIMM은 감정-음향 불일치를 분석하여 위조 음성 탐지에 활용합니다.

## 2.1 Feature Extraction

본 연구에서는 WavLM을 음향 특징 추출에 사용합니다. WavLM은 짧은 발화에 대한 정교한 표현을 제공하며, Wav2Vec2.0 및 HuBERT보다 시간적 맥락 모델링 능력이 뛰어납니다. WavLM은 AASIST 데이터셋으로 미세 조정하여 ADD 작업에 적합한 표현을 얻었습니다. 프레임 수준의 음향 특징은 미세한 지역적 이상을 포착하는 데 사용되며, 발화 수준의 음향 특징은 글로벌 컨텍스트 가중치로 작용하여 지역 감정 변화를 안내하는 역할을 합니다. Emotion2vec은 프레임 및 발화 수준의 감정 특징을 제공하며, 컨트라스티브, 컨텍스트 인식 사전 학습을 통해 시간적 감정 역학을 효과적으로 포착하여 낮은 MSE와 같은 우수한 감정 지표를 제공합니다.

## 2.2 Emotion–Acoustic Alignment Module

본 모듈은 감정 및 음향 특징을 별도의 스트림을 통해 처리하여 중복성을 줄이면서도 주요 변동 정보를 보존하고, 이를 통합된 표현 공간으로 투영합니다.

**음향 표현 스트림(ARS):** 프레임 수준의 음향 특징(facu)을 처리하여 핵심 정보를 유지하면서도 간결한 표현을 생성합니다. SinCNet 레이어는 음향 표현 내의 판별 가능한 주파수 성분을 강화하고, 잔차 블록은 단기적 의존성과 지역적 변동을 포착하여 정교한 음향 역학을 보장합니다. 마지막으로 투영 레이어를 통해 감정 임베딩과 호환되는 표현 공간으로 매핑하여 일관된 입력을 제공합니다.

**감정 표현 스트림(ERS):** 프레임 및 발화 수준의 감정 특징(femo)과 발화 수준의 감정 임베딩(uem)을 처리하여 다양한 척도에서 감정 정보를 보존합니다. 프레임 수준의 감정 특징은 선형 투영과 1D 컨볼루션의 병렬 연산을 통해 중복 정보를 제거하면서도 단기적 의존성과 미세한 감정 변화를 포착합니다. 합산된 출력은 레이어 정규화(LayerNorm)를 통해 정규화되고, 발화 수준의 감정 임베딩은 선형 레이어를 통해 투영되고 레이어 정규화되어 글로벌 감정 추세를 나타내는 안정적인 표현을 제공합니다.

**감정-음향 일치성:** 두 종류의 특징을 공유된 표현 공간에서 일치시키기 위해 다음과 같은 단계를 수행합니다. (1) 동적 불일치 계산: 프레임 수준에서는 연속 프레임 간의 변화를 1차 차분으로 계산하여 동적 변동을 포착하고, 발화 수준에서는 프레임 수준의 감정 특징을 평균하여 발화 수준의 감정 임베딩과 일치시킵니다. (2) 일치성: 계산된 동적 불일치(dfra)와 발화 수준의 불일치(dutt)는 고정된 부호의 이중 헤드 소프트맥스 함수에 입력되어 감정-음향 일치성에 대한 보완적인 가중치를 생성합니다.

## 3 Experiments

기존 연구와의 일관성을 유지하기 위해 AVSspoof 2019LA 및 2021LA 데이터셋에서 본 방법을 평가했습니다. 성능은 EER(Error Rate)과 t-DCF(Time-Domain Cosine Feature)를 사용하여 평가했습니다. 음성 샘플은 64,600개로 표준화되었습니다. 본 모델은 0.83M개의 파라미터를 가지며, NVidia A100 GPU에서 Adam 옵티마이저(학습률 10⁻⁵, weight decay 10⁻⁴)를 사용하여 60 epoch 동안 훈련되었으며, 결과는 3개의 랜덤 시드에 대해 평균을 낸 것입니다.

## 3.1 Main Results

표 1은 2019LA 및 2021LA 데이터셋에서 기존 방법들과 비교한 EAI-ADD의 성능을 보여줍니다. EAI-ADD는 최소 t-DCF 0.0110, EER 0.34%를 달성하여 기존 방법들을 능가하는 성능을 보였습니다. 2021LA 데이터셋에서도 일관된 성능 향상을 보이며, 다양한 위조 공격 조건에서도 강력한 탐지 능력을 유지했습니다.

## 3.2 Ablation Study

표 2는 AVSspoof 2019LA 데이터셋에 대한 Ablation Study 결과를 보여줍니다. 상단 블록은 주요 모듈을 제거한 경우를, 중간 블록은 스트림 및 그래프 변형을 사용한 경우를, 하단 블록은 EVAL에서 시간 창 크기(k)의 영향을 분석한 결과를 나타냅니다. 모든 실험에서 주요 모듈을 제거하거나 스트림 및 그래프 변형을 사용하면 성능이 저하되는 것으로 나타났으며, 이는 각 모듈의 중요성을 입증합니다.

## 4 Conclusions

본 논문에서는 감정-음향 불일치를 주요 탐지 신호로 활용하는 ADD 프레임워크 EAI-ADD를 제시했습니다. EAI-ADD는 감정-음향 일치성 매핑 모듈(EAAM)과 감정-음향 불일치 모델링 모듈(EAIMM)을 통해 감정 및 음향 특징 간의 불일치를 효과적으로 분석하고 활용합니다. AVSspoof 2019LA 및 2021LA 데이터셋에서의 실험 결과는 EAI-ADD가 기존 방법들을 능가하는 우수한 성능을 보임을 입증합니다. 향후 연구에서는 실시간 또는 고압축 조건에서의 실제 환경에서의 검증을 수행할 계획입니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
