# WST-X Series: Wavelet Scattering Transform for Interpretable Speech Deepfake Detection

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2602.02980 · Xi Xuan et al. · 2026

## 한 줄 요약

본 논문에서는 웨이블릿 스캐터링 변환(WST)을 기반으로 하는 새로운 음성 디프페이크 탐지기(WST-X)를 제안하고, 다양한 파라미터 조합에 따른 성능 분석을 통해 작은 평균 스케일과 높은 주파수/방향 해상도가 미세한 특징 포착에 중요함을 밝혔습니다.

## 문제 정의

음성 디프페이크 탐지기는 인공적으로 생성된 음성을 실제 인간 음성과 구별하는 것을 목표로 합니다. 기존의 음성 디프페이크 탐지기(SDD)는 주로 디지털 신호 처리(DSP) 기법이나 자기 지도 학습(SSL) 기법을 활용하는 특징 추출 방식을 사용합니다. DSP 방식은 시간-주파수 분석을 기반으로 하지만, 복잡한 음성 특징을 포착하는 데 한계가 있으며, SSL 방식은 성능은 우수하지만 계산 비용이 높고 해석 가능성이 낮다는 단점이 있습니다. 특히 음성 포렌식 분야에서는 모델의 해석 가능성이 중요하지만, 기존 방식은 이러한 요구사항을 충족시키기 어렵습니다.

## 제안 방법

본 논문에서는 DSP와 SSL 방식의 장점을 결합한 새로운 특징 추출기인 웨이블릿 스캐터링 변환(WST) 기반의 특징 추출기(WST-X)를 제안합니다. WST는 웨이블릿 모듈러스 연산의 캐스케이드로 구성되며, 파라미터(J, Q, M, L)를 조절하여 다양한 스케일과 해상도의 특징을 추출합니다. 1D WST-X는 시간 정보를, 2D WST-X는 시간-주파수 정보를 추출하는 데 사용됩니다. WST는 학습 데이터 없이도 신호의 변환 및 안정성 특성을 활용하여 특징을 추출하며, 계층적인 스캐터링 계수 구조를 통해 다중 스케일 프로세스에 대한 물리적 해석을 제공합니다.

## 실험·결과

본 연구에서는 최근의 어려운 Deepfake-Eval-2024 데이터셋을 사용하여 WST-X의 성능을 평가했습니다. 실험 결과, WSX-X는 기존의 특징 추출 방식보다 월등한 성능을 보였으며, 특히 작은 평균 스케일(J)과 높은 주파수 및 방향 해상도(Q, L)가 미세한 특징을 포착하는 데 중요함을 확인했습니다. WST-X1의 최적 파라미터는 J=2, Q=10, M=2, L=10이며, WST-X2의 최적 파라미터는 J=2, Q=10, M=2, L=10입니다. 안구 해상도(L)가 증가할수록 성능이 향상되는 경향을 보였습니다.

## 한계

본 연구는 Deepfake-Eval-2024 데이터셋에 국한되어 있으며, 실제 환경에서의 다양한 디프페이크 생성 기법에 대한 일반화 가능성은 추가적인 검증이 필요합니다. 또한, WST-X의 파라미터 튜닝에 대한 자동화된 방법론 연구는 아직 부족하며, 다양한 도메인 및 데이터셋에 대한 성능 비교 분석이 필요합니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
