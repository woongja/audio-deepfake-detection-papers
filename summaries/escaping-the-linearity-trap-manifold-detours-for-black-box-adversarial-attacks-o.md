# Escaping the Linearity Trap: Manifold Detours for Black-Box Adversarial Attacks on Singing Audio Deepfake Detection

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2605.30366 · Yifan Liao et al. · 2026

## 한 줄 요약

본 논문에서는 기존의 SSL 기반 노래 음성 위조 탐지(SVDD) 시스템을 우회하는 적대적 공격에 대한 취약성(Linearity Trap)을 발견하고, 이를 해결하기 위해 사전 학습된 SSL 공간과 미끼 음성 특징 공간의 의미 기반 앵커를 활용하는 새로운 블랙박스 적대적 견고성 평가 프레임워크(𝖬𝖠𝖲𝖲\mathsf{MARS})를 제안합니다.

## 문제 정의

최근 노래 음성 합성 기술의 발전은 현실적인 노래 음성을 쉽게 생성할 수 있게 했지만, 동시에 무단 AI 커버와 같은 악의적인 사용의 가능성을 높였습니다. 이러한 위협에 대응하기 위해 노래 음성 위조 탐지(SVDD) 기술이 중요한 방어 수단으로 발전해 왔습니다. 현재 대부분의 고성능 SVDD 시스템은 사전 학습된 음성 SSL 모델과 검출 헤드를 결합하는 Self-Supervised Learning(SSL) 기반 방식을 사용합니다. 그러나 기존의 적대적 공격 방법은 이러한 SSL 기반 시스템의 구조적 특성을 제대로 고려하지 못하여 효과적인 공격을 수행하는 데 어려움을 겪고 있으며, 이는 SVDD 시스템의 견고성에 대한 오해를 불러일으킬 수 있습니다.

## 제안 방법

본 논문에서는 기존 적대적 공격 방법의 한계(Linearity Trap)를 해결하기 위해 𝖬𝖠𝖲𝖲\mathsf{MARS}(Meta-Adversarial Regression of Semantics)라는 새로운 블랙박스 적대적 견고성 평가 프레임워크를 제안합니다. 𝖬𝖠𝖲𝖲\mathsf{MARS}는 다음과 같은 두 가지 주요 전략을 통해 SVDD 시스템의 견고성을 평가하고 개선합니다.

1. **최적화 목표 수준:** 기존 공격 방법이 국소적인 검출기 경계 통과에 집중하는 대신, 사전 학습된 SSL 공간에서 자연어 의미 기반 앵커와 미끼 음성 특징 기반 앵커를 구축하고, 이를 활용하여 미끼 음성 특징을 멀리하면서 자연어 의미를 보존하는 방향으로 공격 목표를 설정합니다.
2. **최적화 방법 수준:** 기존 공격 방법이 미끼 음성 특징 공간의 국소적인 최적화 경로에 갇히는 문제를 해결하기 위해, 듀얼 레벨 최적화 전략을 사용합니다. 내부 루프에서는 미끼 음성 특징 공간의 특정 레이어를 중심으로 탐색하고, 외부 루프에서는 자연어 의미 기반 앵커를 향해 이동하도록 유도하여 검출기의 국소적인 최적화 경로에 얽매이지 않고 전역적인 견고성을 확보합니다.

## 실험·결과

본 논문에서는 ctrSVDD 데이터셋을 사용하여 제안하는 𝖬𝖠𝖲𝖲\mathsf{MARS}의 효과를 검증했습니다. 실험 결과, 𝖬𝖠𝖲𝖲\mathsf{MARS}는 다양한 검출기 및 SSL 모델 조합에서 공격 성공률(ASR)을 크게 향상시키는 것으로 확인되었습니다. 특히, 기존의 대표적인 적대적 공격 방법(PGD, C&W 등)에 대해 높은 ASR을 보였으며, 이는 제안하는 프레임워크가 SVDD 시스템의 구조적 특성을 효과적으로 활용하여 공격을 수행할 수 있음을 시사합니다. 또한, 𝖬𝖠𝖲𝖲\mathsf{MARS}의 핵심 구성 요소인 듀얼 레벨 최적화 전략과 의미 기반 앵커의 중요성을 입증하기 위한 분해 실험(Ablation Study)을 통해, 자연어 의미 만곡선과의 일치성이 공격 성공에 필수적임을 확인했습니다.

## 구현

본 논문에서 제안하는 𝖬𝖠𝖲𝖲\mathsf{MARS}는 SSL 기반 기반 모델과 검출 헤드를 포함하는 일반화된 미끼 음성 특징 네트워크를 구축하여 구현되었습니다. 블랙박스 제약 조건을 엄격히 준수하기 위해 검출 헤드는 ctrSVDD 데이터셋의 훈련 세트를 사용하여 훈련했으며, 공격은 검출기의 로컬 최적화 경로만을 활용하여 수행되었습니다. 듀얼 레벨 최적화 전략은 PGD를 기반으로 구현되었으며, 적절한 탐색 범위와 반복 횟수는 실험적으로 결정되었습니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
