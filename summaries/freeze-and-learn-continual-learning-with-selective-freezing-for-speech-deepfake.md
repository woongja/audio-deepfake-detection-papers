# Freeze and Learn: Continual Learning with Selective Freezing for Speech Deepfake Detection

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2409.17598 · Davide Salvi et al. · 2024

## 한 줄 요약

본 연구는 스피치 디페이크 탐지 모델의 지속 학습을 위해 초기 레이어를 고정하고 후반 레이어만 업데이트하는 방법(CL ℳE\mathcal{M}\_{\text{E}})이 기존 방법보다 효과적임을 실험적으로 입증했습니다.

## 문제 정의

스피치 디페이크 탐지 모델은 새로운 합성 스피치 생성 기술에 적응하면서 기존의 지식을 유지해야 하는 어려움에 직면합니다. 기존의 지속 학습 방법은 모델의 전체 레이어를 업데이트하거나, 특정 레이어만 업데이트하는 방식이 있지만, 어떤 방식이 가장 효과적인지는 명확하지 않습니다. 특히, 모델이 학습한 이전 데이터의 지식을 잃는 카타스트로피 포그팅(catastrophic forgetting) 문제가 발생할 수 있습니다.

## 제안 방법

본 연구에서는 스피치 디페이크 탐지 모델을 인코더 모듈(입력 특징 추출)과 분류 모듈(디페이크 여부 판단)로 분리하여, 초기 레이어(인코더)는 고정하고 후반 레이어(분류)에만 지속 학습 기술(DFWF)을 적용하는 방법을 제안합니다. DFWF는 지식 증류 손실(knowledge distillation loss)과 양성 샘플 정렬(positive sample alignment)을 통해 모델의 이전 지식을 보존하고, 새로운 공격 유형에 대한 적응력을 높입니다.

## 실험·결과

본 연구에서는 RawNet2와 LCNN 모델을 사용하여 AVSpoof2019, FOR, ITW, Purdue의 네 가지 스피치 디페이크 탐지 데이터셋에 대해 다양한 지속 학습 전략을 실험했습니다. 실험 결과, 초기 레이어를 고정하고 후반 레이어에만 지속 학습을 적용한 CL ℳE\mathcal{M}\_{\text{E}} 전략이 가장 높은 성능(평균적으로 81%의 균형 정확도)을 보였습니다. 이는 모델의 입력 특징 분석에 중요한 역할을 하는 초기 레이어의 지식을 유지하면서 새로운 데이터에 적응하는 데 효과적임을 시사합니다. 반면, 전체 모델을 업데이트하거나, 후반 레이어만 업데이트하는 전략은 카타스트로피 포그팅으로 인해 성능 저하를 보였습니다.

## 한계

본 연구는 초기 레이어를 고정하고 후반 레이어에만 지속 학습을 적용하는 전략이 효과적이라는 것을 보여주었지만, 특정 데이터셋이나 모델 구조에 대한 일반화 가능성은 더 추가적인 연구가 필요합니다. 또한, DFWF의 하이퍼파라미터 튜닝이나 다른 지속 학습 기술과의 결합 등은 본 연구에서 다루지 않았으며, 향후 연구의 방향이 될 수 있습니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
