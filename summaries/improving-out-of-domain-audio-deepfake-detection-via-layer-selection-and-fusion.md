# Improving Out-of-Domain Audio Deepfake Detection via Layer Selection and Fusion of SSL-Based Countermeasures

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2509.12003 · Pierre Serrano et al. · 2025

## 한 줄 요약

본 연구는 다양한 사전 학습된 SSL 모델을 고정하고 분류 헤드의 구조를 변경하여 OO(out-of-domain) 환경에서의 오디오 딥페이크 탐지 성능을 분석하고, 최적의 레이어 선택 및 모델 fusion 전략을 통해 성능 향상을 시도했습니다.

## 문제 정의

사전 학습된 SSL(self-supervised learning) 기반 오디오 딥페이크 탐지 시스템은 기존 방법 대비 높은 성능을 보이지만, 학습 데이터와 다른 조건(OO)에서 발생하는 공격에 대한 일반화 성능이 여전히 중요한 과제로 남아있습니다. 특히, 다양한 SSL 모델의 특성과 테스트 데이터셋에 따른 성능 변화를 이해하고, 이러한 문제를 해결하기 위한 효과적인 전략이 필요합니다.

## 제안 방법

본 연구에서는 다양한 사전 학습된 SSL 모델의 레이어별 중요도를 분석하기 위해, 각 레이어의 출력을 mean pooling 헤드에 연결하여 성능 변화를 관찰했습니다. 또한, 여러 레이어의 출력을 결합하는 방법으로 MHFA(multi-head factorized attentive pooling)를 활용하여 분류 헤드에 연결하는 방안을 탐색했습니다. 실험 결과, 최적의 레이어를 선택하거나 여러 모델의 점수를 결합하는 것이 OO 환경에서의 성능 향상에 효과적임을 확인했습니다.

## 실험·결과

다양한 크기와 사전 학습 태스크를 가진 6개의 SSL 모델(BEATS, Wav2vec 2.0, WavLM Base/Large, MMS, Wav2vec2-XLS-R)을 사용했습니다. 각 모델의 레이어를 고정하고, mean pooling과 MHFA 두 가지 분류 헤드를 적용하여 실험을 진행했습니다.

* **레이어별 분석:** 대부분의 모델에서 중간 레이어가 딥페이크 탐지에 가장 중요한 특징을 담고 있는 것으로 나타났습니다.
* **분류 헤드 비교:** 최적의 레이어를 선택하여 mean pooling 헤드에 연결하는 방법과 MHFA를 사용하는 방법 모두 성능 향상에 기여했으며, MHFA는 복잡한 pooling 전략과 유사한 성능을 보면서도 파라미터 수를 크게 줄일 수 있었습니다.
* **모델 fusion:** 여러 SSL 모델의 점수를 결합하는 방법이 다양한 테스트 데이터셋에서 우수한 일반화 성능을 보였습니다. 특히, 간단한 score-level fusion을 통해 상당한 성능 향상을 얻을 수 있었습니다.
* **수치 결과:** InTheWild 데이터셋에서 WavLM Large 모델을 사용한 최적의 레이어 선택 및 MHFA 적용 시 EER 5.4%를 달성했습니다.

## 한계

본 연구는 사전 학습된 SSL 모델을 고정하고 분류 헤드 구조를 변경하는 방식에 집중했으며, 모델 자체의 추가적인 fine-tuning이나 다른 학습 전략은 고려하지 않았습니다. 또한, 공격자가 시스템의 특성을 알고 이를 활용하여 공격을 설계할 가능성은 여전히 존재합니다. 더 나아가, 다양한 SSL 모델과 테스트 데이터셋의 상호작용에 대한 심층적인 이해가 필요하며, 실제 환경에서의 검증을 위한 추가적인 연구가 필요합니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
