# Deepfake Audio Detection Using Spectrogram-based Feature and Ensemble of Deep Learning Models

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2407.01777 · Lam Pham et al. · 2024

## 한 줄 요약

본 논문에서는 다양한 스펙트로그램 기반 특징과 딥러닝 모델을 앙상블하는 새로운 시스템을 제안하며, AVSspoof 2019 벤치마크 데이터셋에서 EER 0.03을 달성하여 최첨단 시스템과 경쟁하는 성능을 보였습니다.

## 문제 정의

딥페이크 오디오는 음성 기반 애플리케이션의 신뢰성과 진위성을 심각하게 위협하는 문제로 부상하고 있습니다. 기존의 딥페이크 오디오 감지 방법은 주로 파이프라인 방식 또는 end-to-end 방식으로 접근하지만, 개별 스펙트로그램과 감지기의 성능이 전체 성능에 미치는 영향에 대한 포괄적인 분석이 부족했습니다. 또한, 특정 데이터셋에 과적합되거나 적대적 공격에 취약한 딥러닝 임베딩 방법의 한계가 존재하며, 사전 학습된 대규모 오디오 모델의 임베딩을 효과적으로 활용하는 연구가 충분히 탐색되지 않았습니다.

## 제안 방법

본 논문에서는 딥페이크 오디오 감지 태스크를 위해 다양한 스펙트로그램 기반 특징과 딥러닝 모델을 앙상블하는 시스템을 제안합니다. 입력 오디오는 2초 세그먼트로 분할된 후, SSTFT, CQT, WT 세 가지 변환 방법을 통해 다양한 스펙트로그램을 생성합니다. 생성된 스펙트로그램은 CNN, RNN, C-RNN, ResNet, MobileNet 등 다양한 딥러닝 모델의 입력으로 사용됩니다. 또한, Whisper, Speechbrain과 같은 사전 학습된 오디오 임베딩 모델을 활용하여 오디오 임베딩을 추출하고, 이를 Multi-Layer Perceptron (MLP) 모델을 통해 분류합니다. 마지막으로, 다양한 스펙트로그램과 딥러닝 모델에서 얻은 결과를 앙상블하여 최종적인 디테크 오디오 감지 성능을 높입니다.

## 실험·결과

제안된 모델은 AVSspoof 2019 벤치마크 데이터셋에서 평가되었습니다. 실험에서는 다양한 스펙트로그램 추출 방법(SSTFT, CQT, WT)과 오디오 필터(Mel, Gammatone, LF, DCT)를 조합하여 스펙트로그램을 생성하고, CNN, RNN, C-RNN, ResNet, MobileNet 등 다양한 딥러닝 모델을 활용했습니다. 또한, Whisper, Speechbrain과 같은 사전 학습된 오디오 임베딩 모델을 사용하여 오디오 임베딩을 추출하고, 이를 MLP 모델로 분류하는 방법을 적용했습니다. 실험 결과, SSTFT 스펙트로그램과 다양한 딥러닝 모델을 앙상블한 모델이 EER 0.03을 달성하여, AVSspoof 2019 챌린지 최고 성능 시스템과 경쟁할 만한 결과를 보였습니다. 특히, 스펙트로그램과 모델을 앙상블하는 것이 단일 스펙트로그램 또는 단일 모델을 사용하는 것보다 성능 향상에 기여하는 것으로 나타났습니다.

## 한계

본 연구는 다양한 스펙트로그램과 딥러닝 모델을 앙상블하는 접근 방식을 제안했지만, 각 구성 요소의 최적 조합에 대한 심층적인 분석은 부족합니다. 또한, 앙상블 모델의 복잡성으로 인해 계산 비용이 증가할 수 있으며, 실제 환경에서의 실시간 적용 가능성에 대한 추가적인 연구가 필요합니다. 더불어, 다양한 공격 기법에 대한 견고성을 평가하고 개선하는 작업은 본 연구의 한계로 지적될 수 있습니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
