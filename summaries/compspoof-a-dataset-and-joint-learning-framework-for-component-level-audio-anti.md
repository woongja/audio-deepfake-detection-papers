# CompSpoof: A Dataset and Joint Learning Framework for Component-Level Audio Anti-spoofing Countermeasures

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2509.15804 · Xueping Zhang et al. · 2025

## 한 줄 요약

본 논문에서는 음성 및 환경 소리 성분 단위의 위조 공격을 탐지하기 위한 새로운 데이터셋(CompSpoof)과 분리 강화된 공동 학습 프레임워크를 제안하며, 실험 결과 기존 방법 대비 우수한 성능을 보였다.

## 문제 정의

기존의 음성 위조 탐지 방법은 전체 발화 또는 시간 영역의 일부에 대한 위조 여부를 이분법적으로 판단하는 경향이 있어, 특정 성분(음성 또는 환경 소리)만 위조된 경우를 정확하게 탐지하기 어렵다. 이러한 한계를 극복하기 위해, 본 연구에서는 음성 및 환경 소리 성분을 분리하여 각 성분별로 위조 여부를 탐지하는 새로운 접근 방식을 제안한다.

## 제안 방법

본 연구에서는 CompSpoof 데이터셋을 활용하여 분리 강화된 공동 학습 프레임워크를 제안한다. 이 프레임워크는 먼저 XLSR-AASIST 모델을 사용하여 입력 오디오를 음성 성분과 환경 소리 성분으로 분리한다. 분리된 각 성분에는 각각 XLSR-AASIST 기반의 anti-spoofing 모델이 적용되며, 이들의 출력을 공동 학습하여 최종 위조 여부를 판단한다. 특히, 분리 과정과 anti-spoofing 모델을 공동으로 학습하여 분리된 성분의 정보를 효과적으로 활용하도록 한다.

## 실험·결과

실험에서는 제안하는 프레임워크(SEF+JL)와 기존의 baseline 모델을 비교 평가했다. CompSpoof 데이터셋의 훈련, 개발, 평가 세트를 사용하여 실험을 진행했으며, 파일 수준의 Precision, Recall, F1 점수를 평가 지표로 사용했다. 실험 결과, SEF+JL은 baseline 모델 대비 전반적으로 높은 성능을 보였으며, 특히 mixed-content 클래스에서 F1 점수가 크게 향상되었다. 또한, 분리된 음성 및 환경 소리에 대한 anti-spoofing 모델의 F1 점수도 공동 학습을 통해 각각 0.863, 0.849로 개선되었다.

## 한계

본 연구에서는 음성 및 환경 소리 성분 분리에 의존하기 때문에, 분리 과정에서 발생하는 오류가 최종 성능에 영향을 미칠 수 있다. 또한, 환경 소리 anti-spoofing 모델의 성능이 음성 anti-spoofing 모델보다 다소 낮게 나타난 것으로 보아, 환경 소리 성분에 대한 별도의 최적화가 필요할 수 있다. 더 나아가, 본 연구는 5개의 클래스로 정의된 component-level 위조 공격만을 다루고 있으며, 실제 환경에서는 더 다양한 형태의 위조 공격이 발생할 수 있다는 점을 고려해야 한다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
