# Mixture of Experts Fusion for Fake Audio Detection Using Frozen wav2vec 2.0

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2409.11909 · Zhiyong Wang et al. · 2024

## 한 줄 요약

본 논문은 pretrained wav2vec 2.0 모델의 가중치를 고정하고 Mixture of Experts fusion을 통해 다양한 layer feature를 효과적으로 통합하여 피싱 오디오 탐지 성능을 향상시키는 방법을 제안합니다.

## 문제 정의

음성 합성 기술의 발전으로 인해 피싱 오디오 탐지가 중요한 과제로 부상했습니다. 기존의 효과적인 피싱 오디오 탐지 방법은 pretrained 모델을 활용하지만, 대부분 fine-tuning 과정을 거쳐야 합니다. 이는 학습 시간 증가 및 새로운 음성 합성 기술에 대한 모델 업데이트를 어렵게 만드는 단점이 있습니다.

## 제안 방법

본 논문에서는 pretrained 모델의 가중치를 고정하고, Mixture of Experts (MoE) fusion 모듈을 제안합니다. MoE fusion 모듈은 gating 네트워크와 여러 개의 expert 네트워크로 구성됩니다. gating 네트워크는 pretrained 모델의 마지막 hidden state를 입력받아 각 expert 네트워크의 활성도를 결정하고, expert 네트워크는 다양한 layer feature를 동적으로 학습하여 통합합니다.

## 실험·결과

ASVspoof2019 및 AVSspoof2021 데이터셋에 대한 실험 결과, 제안된 방법은 fine-tuning을 요구하는 기존 방법과 경쟁력 있는 성능을 보였습니다. 특히, pretrained 모델의 가중치를 고정하고 MoE fusion을 사용하는 것이 fine-tuning만 사용하는 것보다 더 나은 성능을 나타냈습니다. 또한, 제안된 방법은 fine-tuning을 필요로 하지 않아 학습 시간과 계산 비용을 크게 절감할 수 있습니다.

## 한계

본 연구는 pretrained 모델의 마지막 hidden state를 gating 네트워크 입력으로 사용하는 것에 집중했습니다. 다른 layer feature를 활용하거나, MoE fusion 모듈의 구조를 변경하는 등 추가적인 연구를 통해 성능을 더욱 향상시킬 수 있을 것으로 예상됩니다. 또한, 다양한 음성 합성 기술에 대한 일반화 성능을 높이기 위한 연구가 필요합니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
