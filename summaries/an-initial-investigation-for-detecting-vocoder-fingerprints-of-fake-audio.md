# An Initial Investigation for Detecting Vocoder Fingerprints of Fake Audio

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2208.09646 · Xin Yan et al. · 2022

## 한 줄 요약

본 연구는 딥페이크 오디오의 생성 모델(vocoder)을 식별하기 위해 vocoder fingerprints 탐지를 제안하며, 다양한 특징 추출 방법과 딥러닝 모델을 실험적으로 비교 분석하여 LFCC 특징과 ResNet 모델의 우수성을 확인했습니다.

## 문제 정의

딥페이크 오디오의 확산은 사회적 혼란과 보안 위협을 야기하며, 특히 어떤 모델이 해당 오디오를 생성했는지 식별하는 것은 중요한 과제입니다. 기존의 딥페이크 오디오 탐지 연구는 오디오의 진위 여부 판별에 집중되어 왔지만, 생성 모델 식별에 대한 설명이나 대응책은 부족한 실정입니다. 따라서 본 연구는 딥페이크 오디오의 생성 모델을 식별하는 vocoder fingerprints 탐지 문제를 해결하고자 합니다.

## 제안 방법

본 연구에서는 다양한 vocoder의 특징을 효과적으로 추출하고, 이를 기반으로 vocoder fingerprints를 탐지하기 위한 방법을 제안합니다. 먼저, CQCC, MFCC, LFCC 등 다양한 특징 추출 방법을 사용하여 오디오 데이터의 특징을 추출합니다. 다음으로, 추출된 특징을 입력으로 받아 vocoder를 분류하는 X-vector, LCNN, SE-ResNet, ResNet 등 다양한 딥러닝 모델을 실험합니다. 특히, ResNet 모델이 가장 우수한 성능을 보였습니다.

## 실험·결과

실험에서는 eight state-of-the-art vocoders(Multiband-MelGAN, Parallel WaveGAN, LPCNet, Style-MelGAN, WaveNet, HiFiGAN, STRAIGHT, Griffin-Lim)로 생성된 합성 오디오 데이터셋을 사용했습니다. 특징 추출에는 CQCC, MFCC, LFCC를 사용했으며, 딥러닝 모델은 X-vector, LCNN, SE-ResNet, ResNet으로 구성했습니다. 실험 결과, LFCC 특징이 vocoder fingerprints 탐지에 상대적으로 더 효과적이었으며, ResNet 모델이 가장 높은 정확도를 달성했습니다.

## 한계

본 연구는 주로 깨끗한 오디오 데이터에 초점을 맞추고 있으며, 실제 환경에서는 노이즈나 채널 효과가 존재할 수 있습니다. 또한, 현재 연구는 알려진 vocoder에 대한 탐지에 집중되어 있으며, 새로운 유형의 vocoder에 대한 일반화된 탐지 능력은 부족합니다. 향후 연구에서는 노이즈 및 채널 효과에 대한 robustness를 높이고, 새로운 vocoder에 대한 일반화된 탐지 방법을 연구할 필요가 있습니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
