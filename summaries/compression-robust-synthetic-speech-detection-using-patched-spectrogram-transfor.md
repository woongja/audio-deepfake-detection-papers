# Compression Robust Synthetic Speech Detection Using Patched Spectrogram Transformer

> ⚠ AI-generated summary (local LLM, **Korean**, unverified) — auto-generated from the paper, not fact-checked. Verify against the source.

**arXiv:** https://arxiv.org/abs/2402.14205 · Amit Kumar Singh Yadav et al. · 2024

## 한 줄 요약

본 논문에서는 합성 음성 감지를 위해 패치 기반 Mel-spectrogram 처리를 수행하는 Transformer 기반의 새로운 모델(PS3DT)을 제안하고, 다양한 데이터셋과 압축 조건에서 PS3DT의 성능을 검증하여 기존 방법들보다 우수함을 보였습니다.

## 문제 정의

합성 음성 생성 도구의 발전으로 인해 금융 사기, 개인 사칭, 허위 정보 유포 등 심각한 사회적 문제가 발생하고 있습니다. 이러한 문제를 해결하기 위해 합성 음성을 감지하는 forensic 방법이 필요하며, 기존 방법들은 특정 데이터셋에 과적합되어 실제 환경(예: 소셜 미디어)에서 성능이 저하되는 경향이 있습니다. 특히, 소셜 미디어 플랫폼에서는 음성 신호를 손실 압축(lossy compression) 방식으로 전송하는 경우가 많으므로, 이러한 압축 조건에서도 견고하게 합성 음성을 감지할 수 있는 기술이 요구됩니다.

## 제안 방법

본 논문에서는 합성 음성 감지를 위해 패치 기반 Mel-spectrogram 처리를 수행하는 Patched Spectrogram Synthetic Speech Detection Transformer (PS3DT) 모델을 제안합니다. PS3DT는 먼저 시간 영역 음성 신호를 Mel-spectrogram으로 변환한 후, 이를 25ms 간격의 패치로 분할합니다. 이후 동일한 시간 위치에 해당하는 패치 표현들을 합쳐 프레임 표현을 생성하고, 이 프레임 표현을 Transformer 네트워크에 입력하여 합성 음성 여부를 판단합니다.

## 실험·결과

본 논문에서는 PS3DT 모델의 성능을 평가하기 위해 ASVspoof2019, In-the-Wild, ASVspoof2021 세 개의 데이터셋을 사용했습니다.

**ASVspoof2019 데이터셋:** PS3DT는 ASVspoof2019 데이터셋에서 기존의 spectrogram 기반 및 Mel-spectrogram 기반 합성 음성 감지 방법들과 비교하여 우수한 성능을 보였습니다. 특히, Mel-spectrogram을 사용하는 PaSST 모델보다 낮은 EER(Equal Error Rate)를 달성했습니다.

**In-the-Wild 데이터셋:** PS3DT는 ASVspoof2019 데이터셋에서 학습된 다른 방법들에 비해 In-the-Wild 데이터셋에서 더 나은 일반화 성능을 보였습니다. 이는 PS3DT가 특정 데이터셋에 과적합되는 경향을 줄이고, 다양한 환경의 합성 음성에 대해 더 robust하게 감지할 수 있음을 시사합니다.

**ASVspoof2021 데이터셋:** ASVspoof2021 데이터셋에서 PS3DT는 전화 통신 환경과 같이 압축된 합성 음성 신호에 대해서도 기존 방법들보다 더 강력한 견고성을 보였습니다. 특히, 전화 품질의 합성 음성 감지에서 PS3DT는 다른 방법들보다 낮은 EER을 달성했습니다. 또한, 소셜 미디어 플랫폼에서 흔히 사용되는 압축 방식(AAC, OPUS, GSM)으로 압축된 합성 음성에 대해서도 높은 감지 성능을 유지했습니다.

## 한계

본 연구에서는 PS3DT 모델의 성능을 다양한 데이터셋과 압축 조건에서 평가했지만, 실제 환경에서의 다양한 노이즈 및 변형에 대한 robustness 측면에서는 추가적인 연구가 필요합니다. 또한, 모델의 계산 복잡도가 높을 수 있으며, 실시간 처리 성능 개선을 위한 연구가 필요할 수 있습니다.

---
_Part of [audio-deepfake-detection-papers](https://github.com/woongja/audio-deepfake-detection-papers) · summary auto-generated by a local LLM, unverified._
